# Crop Screenshots

Crop UI chrome (tab bars, status bars, toolbars) from a directory of PNG screenshots in one call.

## Run

```bash
bal run
```

Place your PNG files inside a `screenshots/` directory next to `main.bal` before running.
