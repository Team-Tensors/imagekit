# Batch Convert PNG to JPEG

Convert all PNG files in a directory to JPEG and resize them to fit within 1280×720,
preserving aspect ratio.

## Run

```bash
bal run
```

Place your PNG files inside an `input/` directory next to `main.bal` before running.
Output JPEG files are written to `output/`.
