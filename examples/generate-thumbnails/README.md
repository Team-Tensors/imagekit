# Generate Thumbnails

Batch-generate 200×200 thumbnails for all PNG and JPEG images in a directory.
Thumbnails preserve the original aspect ratio.

## Run

```bash
bal run
```

Place your images inside an `images/` directory next to `main.bal` before running.
Thumbnails are written to `thumbnails/`.
